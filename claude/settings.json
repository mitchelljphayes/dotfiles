{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "$HOME/.dotfiles/claude/hooks/block-generated-branch-push.sh",
            "timeout": 10,
            "statusMessage": "Checking branch name before push"
          }
        ]
      }
    ]
  },
  "statusLine": {
    "type": "command",
    "command": "input=$(cat); cwd=$(echo \"$input\" | jq -r '.workspace.current_dir'); model=$(echo \"$input\" | jq -r '.model.display_name'); git_info=\"\"; if git -C \"$cwd\" rev-parse --is-inside-work-tree >/dev/null 2>&1; then branch=$(git -C \"$cwd\" branch --show-current 2>/dev/null || git -C \"$cwd\" rev-parse --short HEAD 2>/dev/null); if [ -n \"$branch\" ]; then git_info=\" on $branch\"; fi; fi; printf \"\\033[2m%s in %s%s | %s\\033[0m\" \"$(whoami)\" \"$(basename \"$cwd\")\" \"$git_info\" \"$model\""
  },
  "extraKnownMarketplaces": {
    "impeccable": {
      "source": {
        "source": "git",
        "url": "https://github.com/pbakaus/impeccable.git"
      }
    }
  },
  "skipWorkflowUsageWarning": true,
  "outputStyle": "Plain Prose",
  "editorMode": "vim",
  "feedbackSurveyState": {
    "lastShownTime": 1754194786480
  },
  "enabledPlugins": {
    "impeccable@impeccable": true
  }
}
