---
description: Create a well-crafted commit for current changes
---

Create a well-crafted commit for the current changes.

1. Check if this is a GitButler-managed repository (`but status`)
   - If GitButler: use `but commit` commands
   - If standard git: use normal git workflow
2. Analyze staged/unstaged changes
3. Generate conventional commit message
4. Update CHANGELOG.md if present
5. Show proposed commit for approval

**Wait for my approval before committing.**
