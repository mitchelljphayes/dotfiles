# 1Password secret references for opencode MCP servers
# These are resolved at runtime by `op run`
# See: https://developer.1password.com/docs/cli/secrets-environment-variables
#
# Work-specific secret references are in the private dotfiles-work repo.
# Add personal (non-employer) MCP secrets here.

# TODO: Add Google OAuth credentials when setting up workspace-mcp
# Note: 1Password secret references can't have parentheses in item names